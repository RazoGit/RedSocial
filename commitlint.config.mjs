/**
 * Conventional Commits: https://www.conventionalcommits.org/es/
 * Ejemplos: feat(auth): login social con Google | fix(feed): cursor paginación
 */
const config = {
  extends: ["@commitlint/config-conventional"],
};

export default config;
